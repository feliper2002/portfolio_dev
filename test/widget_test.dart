import 'package:flutter_test/flutter_test.dart';

import 'package:feliper_developer/main.dart';

void main() {
  testWidgets('', (WidgetTester tester) async {
    await tester.pumpWidget(InitApp());
  });
}
